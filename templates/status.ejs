<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="css/status.css">
    <title>Server Status</title>
</head>
<body>     
    <nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid d-flex justify-content-start">
            <a class="navbar-brand mb-0 h1" href="/">TheIT.Blog</a>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/status">Server Status</a>
                </li>
            </ul>
            <div class="ms-auto">
                <button class="btn btn-outline-success" type="button" onclick="window.location.href = '/login'">Admin Login</button>
            </div>
        </div>
    </nav>
    <h3 style="margin-top: 5%; text-align: center;">Server Status</h3>
    <div class="card" style="width: 23rem; margin: 80px auto; margin-top: 5%;">
        <div class="card-body" style="text-align: center; justify-content: center;">
          <h5 class="card-title">MySQL Database</h5>
          <p class="card-text" id="database-status" style="font-size: 1.25rem; font-weight: bold; margin-bottom: 15px;"><%= status %></p>
          <a href="#" class="btn btn-primary">Implementable later to refresh if admin</a>
        </div>
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            let status = document.getElementById('database-status');
            if(String(status).trim() === "Online")
                status.style.color = "#a83232";
            else
                status.style.color = "#32a858";
        });
    </script>
    <script src="web_common.js"></script>
</body>
</html>